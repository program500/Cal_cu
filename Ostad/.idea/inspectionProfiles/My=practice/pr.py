#argument type data and paramitars type data

def my_function(a, b):
    x = a 
    y = b 
    sum = x + y 
    sub = x  - b 
    mul = x * y 
    div = x / y 
    modu = x % y 
    print( sub, sum , mul, div, modu)
my_function(10, 5)
